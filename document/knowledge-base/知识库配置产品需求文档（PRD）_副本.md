# 知识库配置研发需求边界

> 版本：v1.2
>
> 更新日期：2026-07-30
>
> 需求依据：《知识库体系架构方案》《通用检索系统产品方案》《rag方案（调研）》《企业网站行业知识库分类体系说明》及配套知识归类模板
>
> 文档使用方式：第六、七节说明功能与交互，第八节说明指标和状态口径，第九节说明业务对象；第十一、十二节供测试和验收使用。

## 一、文档说明

**这份文档记录什么**：知识库配置模块的研发需求边界——页面、字段、交互、指标口径和业务对象。本文以知识库体系、检索方案和现有知识库 PRD 为内容依据，将已确认的页面交互收敛为可实现、可测试的规则、状态、接口语义和验收标准，不是愿景稿。

**和另外三份文档的关系**：

| 文档 | 定位 | 与本文档的关系 |
|---|---|---|
| 《知识库体系架构方案》 | 内容组织的设计源头：6 大板块怎么分类、生命周期、健康度、治理 | 本文档里"知识板块""健康度""门禁检查"等概念均出自该方案，本文档只取其中**已经做成界面**的部分 |
| 《通用检索系统产品方案》 | 检索能力的设计源头：双模检索、5 类数据源、参数体系 | 本文档"检索策略""问答测试"对应该方案的落地 |
| 《rag方案（调研）》 | 全链路 RAG 深化调研 | 影响了切片解析方式（自动元数据/关键词/问题提取等字段，参考 RAGFlow） |
| **本文档** | 已实现原型的 PRD | 面向研发排期/评审，说明页面能力、交互逻辑和指标口径 |

**读者**：研发（对照原型排期开发）、测试（对照功能清单验收）。

**目标用户**：网站运营/内容运营人员，非技术背景。所以整个模块的设计原则是"能上传/能改字段就行"，复杂的解析算法、检索参数系统给出**推荐默认值**，藏在"配置"里，日常不需要碰。

---

## 二、产品定位与目标

网站配了智能客服和内容生成智能体后，两者都要"读知识"才能干活——客服要引用知识回答用户，内容生成要引用知识写文案。这些知识散落在前台页面、文件、外部网页、数据库里，格式不一、更新方式不一。

**知识库配置**要解决的问题：让运营人员用一个后台，把这些分散的内容**统一管起来**——知道有什么知识、知道好不好用、知道怎么让它更好用，而不需要理解背后的向量库/分块算法/RAG 是什么。

一句话定位：**知识库配置 = 网站知识的中央管理后台**，管内容、管处理方式、管效果。

---

## 三、整体架构图

```
┌───────────────────────────────────────────────────────────────────────────┐
│                          console-crm 中台原型                                │
│                                                                             │
│   ┌─────────────────┐   引用知识回答    ┌──────────────────────────────┐    │
│   │ N3 智能体配置     │ ◄──────────────  │                              │    │
│   │（客服/营销智能体）│                  │                              │    │
│   └─────────────────┘                  │      知识库配置模块            │    │
│                                          │     /knowledge-config        │    │
│   ┌─────────────────┐   引用知识生成    │                              │    │
│   │ 内容生成智能体     │ ◄──────────────  │  统一管理网站全部知识内容：     │    │
│   │（营销文案/落地页）│                  │  是什么、好不好用、怎么用更好   │    │
│   └─────────────────┘                  │                              │    │
│                                          └──────────────────────────────┘    │
│                                                     │  ▲                     │
│                                        内容来自      │  │ 内容/参数           │
│                                                     ▼  │                     │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │  网站内容来源：前台页面结构 / 手动上传文件 / 外部网址 / 对象存储 / AI 生成回流 │   │
│   └───────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼  设计依据
        《知识库体系架构方案》（内容分类/生命周期/健康度）
        《通用检索系统产品方案》（检索参数/双模调控）
```

**模块内部三层**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 外壳层  顶部标题条 + 二级左侧导航（4 组 7 个入口）+ 内容区              │
├─────────────────────────────────────────────────────────────────┤
│ 页面层  7 个独立页面，见「四、功能架构图」                             │
├─────────────────────────────────────────────────────────────────┤
│ 数据层  知识内容、同步状态、对话记录、健康度、覆盖盲区等业务数据          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、功能架构图（信息架构）

```
知识库配置 /knowledge-config
│
├─ 总览
│   （运营看板：关键指标 + 待办 + 板块分布）
│
├─ 知识内容
│   ├─ 知识板块（6 类卡片列表）
│   │   └─ 板块配置（点 ⚙️ 进入）
│   │       ├─ 切片解析方式
│   │       ├─ 入库前清洗与质量评估策略
│   │       ├─ 检索策略
│   │       └─ 内容概览（只读监控）
│   │
│   ├─ 知识资产目录（内容管理主战场）
│   │   └─ 知识详情（点条目进入）
│   │       ├─ 元数据 + 标签
│   │       ├─ 健康度分解
│   │       ├─ 更新历史
│   │       ├─ 同步/抓取（如适用）
│   │       ├─ 内容（原文 + 解析切片，双栏）
│   │       ├─ 入库前门禁检查
│   │       ├─ 检索参数摘要
│   │       └─ 来源
│   │
│   └─ 元数据规范
│       （文档级元数据字段与选项配置 + 板块元数据总览 + 门户网站范围选择 + 全局枚举维护）
│
├─ 数据接入
│   └─ 同步状态
│       （所有同步方式的运行情况：实时/定时/手动）
│
└─ 检索与运营
    ├─ 问答测试
    │   （像用户一样提问，验证板块处理效果）
    └─ 对话质量运营
        （从当前客户智能客服对话中抽指标：召回、回复、准确率、满意度、盲区）
```

---

## 五、用户使用流程图

### 流程 A · 日常内容录入与维护（最高频）

```
进入「知识资产目录」
        │
        ▼
点「+ 新增知识」──┬─► 上传文件（多选，一次建多条）
                 ├─► 外部网址（可加多个，支持"来源分组"聚合展示）
                 ├─► 对象存储（可勾选"同时归入其它板块"）
                 └─► 从前台页面选择（勾选前台导航里的页面，可改归属板块）
        │
        ▼
内容自动落入对应「知识板块」──（板块已配好处理方式，无需再选解析参数）
        │
        ▼
可选：在列表里直接编辑元数据（行业/门户网站/标签/业务用途…）
也可查看/补充前台页面路由、导航路径、页面数据类型、结构化字段
或归到「知识目录」文件夹分组，方便按业务口径归类
        │
        ▼
点条目进「知识详情」──┬─► 看解析出的切片对不对（原文 vs 切片双栏对照）
                     ├─► 切片可加关键词/问题/标签、启用禁用、批量操作
                     └─► 看健康度、门禁检查有没有卡住
        │
        ▼
若来源是前台页面/外部网址/对象存储 → 会自动保持同步，去「同步状态」看运行是否正常
```

### 流程 B · 板块处理效果调优（低频，出问题时）

```
「问答测试」里模拟用户提问，选定「测试范围」缩小到某个板块
        │
        ▼
发现答非所问 / 没召回 ──► 判断是内容问题还是参数问题
        │                          │
        ▼ 内容问题                  ▼ 参数问题
  去「知识资产目录」          去「知识板块」→ 点 ⚙️ 进入该板块配置
  补充/修正对应知识                  │
                                    ▼
                     调整「切片解析方式」（分块粒度/PDF解析器/自动关键词提取…）
                     或「入库前清洗与质量评估策略」（敏感词/去重/打分阈值）
                     或「检索策略」（向量权重/阈值/TopK/Rerank）
                                    │
                                    ▼
                          保存配置 → 已入库内容需「重新学习」才应用新配置
                                    │
                                    ▼
                          回「问答测试」重新验证
```

### 流程 C · 效果监控与运营分析（周期性）

```
「对话质量运营」看智能客服前台对话情况
        │
        ├─► KPI：对话轮次 / 知识库召回率 / AI 回复率 / RAG 准确率 / 满意度
        ├─► 对话记录：按场景筛选，点开看原始对话
        └─► 覆盖盲区：从未命中/未回复对话聚类出待补问题，点「去补录」跳回录入
        │
        ▼
「元数据规范」里看「板块元数据总览」表（可排序）
        │
        └─► 按"引用次数"排序，找出高频板块 → 优先投入维护资源
```

### 流程 D · 目录内知识健康体检（内容库存体检）

```
「知识资产目录」
        │
        ├─► 点「新建体检任务」，进入体检范围选择状态
        ├─► 按板块 / 来源方式 / 行业 / 时效 / 权限等条件筛选，再勾选要检查的知识资产
        ├─► 点「加入体检任务」，后台异步执行，任务进入体检记录
        ├─► 在「体检记录」查看已完成任务及检测结果：有用 / 低复用 / 需更新 / 无命中 / 缺路由 / 缺结构化字段
        └─► 从单条结果进入详情，执行同步、重新学习、补齐元数据、下线等修复动作
        │
        ▼
「对话质量运营」继续负责召回、回复、准确率、满意度、盲区等对话侧指标
```

---

## 六、功能清单

### 6.1 本期功能范围（按当前知识库原型重整）

本表是本期功能范围的总览，按运营用户实际完成的工作组织，而不是按技术实现拆分。知识健康体检已并入知识资产目录，不是独立导航、路由或页面。

| 工作域 | 页面/入口 | 本期交付 | 明确边界 |
|---|---|---|---|
| 看全局 | 概览 | 知识总数、已发布、待处理、平均健康度；待评估、低质、待更新、覆盖盲区待办及跳转 | 只展示知识库存和当前客户的运营待办，不做跨客户看板。 |
| 定规则 | 知识板块、板块配置 | 固定 6 类板块；配置解析、清洗/门禁、检索与只读内容概览 | 不支持新增或删除板块；配置变更需通过重新学习作用于已有资产。 |
| 管知识 | 知识资产目录、知识详情 | 新增、编辑、分组、筛选、状态迁移、内容/切片维护、来源追溯、门禁、同步和更新历史 | 这是唯一的知识内容增删改入口；板块页只管规则，不能重复编辑内容。 |
| 接前台页面 | 目录中的“从前台页面选择” | 展示全部站点、导航和前台页面；批量入库并带入页面 ID、路由、导航路径、页面类型、数据类型、结构化字段 | 面向运营只称“前台网站结构/前台页面”；不暴露页面设计器或 CMS 的实现概念。 |
| 做健康体检 | 目录中的体检面板、范围选择、体检记录、结果详情 | 内容库存与前台问答效果合并指标；按筛选勾选创建异步任务；查看任务历史、单条诊断和修复动作 | 体检范围与普通批量选择隔离；任务历史和检测快照保留，修复后展示当前状态。 |
| 管元数据 | 元数据规范 | 板块总览、健康权重/高级参数、全局枚举、敏感词、门户范围、统一字段 | 内置字段不可删除；配置保存后按生效规则影响后续评估或重新学习。 |
| 保持更新 | 同步状态 | 实时/定时/手动三类同步、近一周默认视图、日志、失败重试及同机制批量操作 | 只允许同一同步机制一起批量操作；同步失败不刷新内容更新时间。 |
| 验证与运营 | 问答测试、对话质量运营 | 板块范围问答测试；当前客户的召回、回复、准确率、满意度、盲区和补录闭环 | 问答测试是隔离预览，不写入正式调用、健康度或盲区指标。 |

### 6.2 页面功能明细

| 模块 | 页面 | 功能点 | 说明 |
|---|---|---|---|
| 总览 | 概览 | 关键指标看板 | 知识总数/已发布/待处理/平均健康度 4 个 KPI |
| | | 待办列表 | 待评估、低质（健康度<60）、内容过期、覆盖盲区，各带一键跳转 |
| | | 快捷操作 | 录入知识 / 测试机器人回答 / 查看板块处理方式 |
| | | 板块内容分布 | 6 个板块的条数+健康度横向对比条，点击跳转筛选后的资产目录 |
| 知识板块 | 板块列表 | 6 类板块卡片 | 展示定义、典型内容、怎么处理/怎么检索、条数、健康度 |
| | | 入口分流 | 点卡片→资产目录（按板块筛选）；点 ⚙️ →该板块配置页 |
| | 板块配置 | 切片解析方式 | 分块策略、任务页面大小、PDF 解析器、文本块大小、分段标识符、子文本块检索、PageIndex、图像表格上下文窗口、自动元数据（含「元数据生成设置」弹窗）、自动关键词提取、自动问题提取、语义感知分块，均带说明 tooltip |
| | | 入库前清洗与质量评估策略 | 敏感词过滤（对照可维护敏感词库）、去重（标题+标签相似度算法，阈值60%）、质量打分拦截阈值（分数=同一套质量公式，见 §8.4），三项均驱动门禁检查的判定依据，不是孤立开关 |
| | | 检索策略 | 向量权重、相似度阈值、TopK、Rerank、Embedding 模型 |
| | | 内容概览（只读） | 该板块条数、状态分布、同步失败清单（可直接"立即同步"）、平均健康度 |
| 知识资产目录 | 列表 | 搜索/筛选 | 按名称搜索；按板块/来源方式/状态/行业/时效/权限筛选 |
| | | 多选与批量操作 | 支持多选行（checkbox），批量状态变更（仅当所选知识状态一致时可用，显示合法的下一步动作按钮），批量操作区域出现在表单上方 |
| | | 知识健康体检 | 在知识资产目录顶部合并为一个体检指标面板，按「内容库存」和「前台问答效果」分组展示知识总数、待更新、最近更新时间、待补 FAQ、知识库召回率、AI 回复率、回复率损耗、总检索次数、命中次数、未回答数；点「新建体检任务」进入范围选择状态，运营可先筛选再勾选知识并加入任务；任务后台执行，已完成任务在「体检记录」中长期可查看；每条结果可进入详情，并在详情内执行补路由、补结构化字段、同步、重新学习、标记已更新、下线等修复动作 |
| | | 知识目录分组 | 人工文件夹分组（新建/归类），纯归类用途，与处理方式无关 |
| | | 知识库召回率 KPI | 不再单独占一排大卡片，合并到「知识健康体检」的「前台问答效果」指标组中展示 |
| | | 新增知识（Tab切换式） | 弹窗顶部选择板块后，根据板块类型显示不同Tab选项：②问答知识（手动输入问答推荐+批量上传FAQ）；③结构化数据（引导至前台页面选择）；④AI生成内容（上传+外部库）；⑤网站知识（外部网址推荐+上传+对象存储）；⑥行业知识（对象存储推荐+上传+外部网址）；①文档知识（上传推荐+外部网址+对象存储）。Tab带蓝色下划线高亮，推荐Tab标"推荐"徽章，切换板块自动切到该板块默认Tab |
| | | 从前台页面选择 | 按站点、完整导航树展示全部前台页面；每个页面带页面 ID、路由、导航路径、页面类型、页面数据类型、结构化字段数和推荐板块，运营可勾选入库并逐条调整归属板块。导航结构默认学习，其它页面内容按结构化数据保存；后台拿到的是前台网站结构，不要求用户理解内部生成过程 |
| | | 列表字段 | 多选checkbox、前台页面/路由/导航路径/数据类型、板块、来源方式、标签、适用站点、行业、时效、权限、状态、健康度、创建/更新时间、是否开启 |
| | | 行内操作 | 编辑、同步（如适用）、重新学习、删除 |
| | | 编辑弹窗 | 全字段编辑，含所属分组、备注、状态操作（当前状态+合法下一步动作按钮，见 §8.2） |
| | | 未回答问题转移 | 列出高频未命中问题，一键转移待补录 |
| | 知识详情 | 元数据展示 | 板块、来源方式、适用站点、行业、业务用途、场景、时效、权限、状态、标签、备注 |
| | | 状态操作 | 顶部操作区展示当前状态+合法下一步动作按钮（发布/下线/标记已更新等），点击真实改变状态并联动重算健康度（见 §8.2） |
| | | 健康度分解 | 质量/时效/复用/反馈四维，每维带计算口径 tooltip，低分给改善建议，权重动态读取当前配置（见 §8.1） |
| | | 更新历史 | 创建/编辑/重新学习等操作时间线 |
| | | 同步/抓取信息 | 同步方式、频率、上次/下次时间、状态，可立即同步 |
| | | 内容双栏预览 | 左：原文；右：解析切片列表，可搜索/筛选 |
| | | 切片编辑 | 点切片开编辑弹窗：正文、Type、关键词、问题、标签、启用开关 |
| | | 切片批量操作 | 多选后批量启用/禁用/删除 |
| | | 入库前门禁检查 | 7 项检查逐项展示通过/未通过 + 具体判定依据（命中的敏感词、相似的资产名、切片问题数等，见 §8.4）；②问答知识板块可在此"标记已人工审核" |
| | | 检索参数摘要 | 只读展示该板块的检索参数，带跳转配置入口 |
| | | 来源展示 | 回答时对外展示的出处引用 |
| 元数据规范 | | 板块元数据总览 | 6 个板块的条数/健康度/引用次数/自定义字段数/最近更新，列头可排序，带跳转配置入口；引用次数可切统计时间范围（今天/7天/30天/全部，默认全部） |
| | | 健康度评分权重 | 质量/时效/复用/反馈四项滑块（须和为100），带每项计算口径说明，调整后实时预览各板块重算后的平均健康度（见 §8.1） |
| | | 全局枚举维护 | 行业标签、业务用途、适用场景，可增删 |
| | | 固定枚举展示 | 权限级别、时效等级（系统固定，只读） |
| | | 敏感词库 | 合规检查用词库，可增删（见 §8.4），供各板块"敏感词过滤"检查项使用 |
| | | 门户网站范围 | 选择元数据规范适用的门户网站；门户网站本身由站点配置提供，本页不负责新建/删除 |
| | | 统一元数据字段 | 内置字段（与状态机/健康度/检索等系统逻辑绑定）只读展示；自定义字段全部板块通用，可增删改，加入后自动出现在「知识资产目录」编辑弹窗与「知识详情」元数据卡片 |
| 同步状态 | | 默认展示与更多按钮 | 默认展示近一周数据，右上角"更多"按钮查看全部历史 |
| | | 筛选条件 | 按板块/来源方式/同步状态筛选（与知识资产目录字段保持一致，减少维护成本） |
| | | 按同步方式概览 | 实时同步/定时同步/手动 三组数量统计 |
| | | 状态分布 KPI | 正常/同步中/失败/未启用 四个计数 |
| | | 失败告警 | 有失败项时置顶提示 |
| | | 统一列表 | 内容标题/板块/来源方式/状态/健康度/频率（可调）/上次同步/下次同步/同步状态，点击行跳转知识详情；同一同步类型（实时同步/定时同步/手动）可多选 |
| | | 行内/批量操作 | 单条立即同步（失败态显示"重试"）、查看日志；多选同类型内容后可批量立即同步/批量重试，定时同步类型还可批量调整频率 |
| 问答测试 | | 回答风格切换 | 客服模式 / 营销模式 |
| | | 匹配严格度 | 宽松/标准/严格三档，对应不同相似度阈值 |
| | | 测试范围 | 板块多选 chip，可缩小到指定板块测试 |
| | | 对话交互 | 提问→回答，示例问题快捷发送，可清空对话 |
| | | 引用来源展示 | 每条命中带板块、相似度、相关性/权威性/时效性多维评分、出处 |
| 对话质量运营 | | 页面定位 | 当前客户后台的对话分析页，回答"智能客服前台哪些对话没答好、为什么没答好、下一步补什么知识" |
| | | 时间范围筛选 | 今天/近7天/近30天/全部（默认近7天），对话轮次/知识库召回率/AI 回复率/RAG 准确率/满意度随之重算，盲区数是当前快照不受影响（见 §8.3） |
| | | 核心 KPI | 对话轮次、知识库召回率、AI 回复率、RAG 准确率、满意度、盲区数（快照），每项在页面内展示计算规则 |
| | | 筛选维度 | 按时间、场景（在线客服/网站搜索/内容生成）、命中知识名称筛选；不提供客户/行业筛选，因为当前后台只属于一个客户 |
| | | AI 质量指标（新增） | **知识库召回率**（检索是否命中相关知识，已有）、**AI 回复率**（召回后是否给出回答，≈87%，已有）、**RAG 准确率**（命中且未点踩/未转人工 = 答得对，新增）、**回复率损耗归因**（解释召回率 99% → 回复率 87% 的 12pt 损耗分布，新增） |
| | | 场景统计 | 在线客服/网站搜索/内容生成 三场景的调用量+召回率（全量参考分布，独立于时间范围） |
| | | 调用记录（默认近一周+更多） | 日期/场景/问题/命中知识/相似度/反馈/是否回复/未回复原因/是否转人工/引用片段，可按场景筛选、按命中知识名称搜索，可查看原始对话；默认展示近一周，右上角"更多"按钮查看全部 |
| | | 覆盖盲区（默认近一周+更多） | 未命中问题聚类、本客户出现次数、影响会话数、最近提问时间、建议板块、处理状态（待处理/已补录/已忽略）、优先级；默认展示近一周，右上角"更多"按钮查看全部；操作：补录（跳转至知识资产目录，预置板块为"问答知识"，带上问题内容）、忽略（标记不再提示） |

---

## 七、功能描述

### 7.1 总览（概览）

运营人员进入模块的第一屏，回答"现在整体情况怎么样、我下一步该干什么"。4 个 KPI 卡给全局印象；"需要处理"列表把待评估、低健康度、过期待更新、覆盖盲区四类事项聚合成待办，每条都能一键跳到对应页面处理，不需要自己去各页面翻找。板块分布用横向条形图对比 6 个板块的知识量和健康度，点击某一行直接跳到「知识资产目录」并预置该板块筛选。

4 个 KPI 的口径固定：知识总数为未删除资产数；已发布上线为已发布且启用检索的资产数；待处理为待评估、待更新、低健康度（健康度低于 60）三类待办的合计，允许同一资产同时计入多个待办；平均健康度只计算已有健康度的资产，没有可计算资产时显示 `--`。概览跳转只带入筛选条件，不自动修改资产或创建体检任务。

### 7.2 知识板块

**列表页**是 6 类知识处理方式的说明书：每张卡片用大白话解释"这类知识怎么处理、怎么检索"，不出现分块算法、向量库这类术语。卡片本身点击进入资产目录（内容），卡片右上角单独一个 ⚙️ 图标进入配置（处理方式），两个入口职责分开，避免运营人员在"看内容"和"改参数"之间迷路。

**配置页**是唯一改处理方式的地方，分三段：

- **切片解析方式**：参考 RAGFlow 的通用切片方法（任务页面大小、PDF 解析器、文本块大小等），每个配置项都带 `?` 提示解释含义和影响；"自动元数据"额外提供「设置」入口，弹窗里可以像 RAGFlow 一样定义要自动生成的自定义字段（名称/描述/类型），分「生成」（用户自定义）和「内置」（系统固定，如标题、来源、创建时间）两个标签页。
- **入库前清洗与质量评估策略**：敏感词过滤、去重策略、质量打分拦截阈值——这是内容进库前的"质检关卡"，对应架构文档里的门禁检查在板块层面的可配置化。
- **检索策略**：向量权重、相似度阈值、TopK、Rerank、Embedding 模型，系统给出行业推荐默认值，页面上直接提示"仅在出现具体召回问题时再调整"，降低误调的可能。
- **内容概览**：纯只读的监控视图（条数、状态分布、同步失败清单、平均健康度），不提供编辑能力——editing 内容始终只在「知识资产目录」做，避免同一件事有两个入口造成数据不一致的心智负担。

结构化数据板块（③）特殊处理：不进向量库，直连数据库查询，所以配置页对这个板块只展示说明文字，跳过三段可配置表单。

其余板块保存配置时需校验数值范围：任务页面大小不少于 1，文本块大小为 64–2048 且按 64 递增，图像/表格上下文窗口为 0–5，自动关键词/问题提取数量为 0–10，向量权重和相似度阈值为 0–1，Top K 为 1–20。配置保存只影响后续入库；已有内容必须重新学习成功后才应用新配置。

### 7.3 知识资产目录

这是模块里**唯一**的内容增删改入口，定位是"用户只需要传东西"的极简录入体验。

**Tab切换式新增交互**：打开新增知识弹窗后，根据所选板块智能显示对应的Tab导航，每个板块有不同的Tab选项组合：

- **②问答知识（faq）**：
  - Tab 1：✍️ 手动输入问答（推荐，默认）— 填写问题+答案，直接创建草稿
  - Tab 2：📤 批量上传FAQ — 上传FAQ文档
  
- **③结构化数据（structured）**：
  - 无Tab，直接引导至"从前台页面选择"（读取前台页面绑定的结构化字段）

- **④AI生成内容（content）**：
  - Tab 1：📤 上传生成内容（默认）
  - Tab 2：🔗 外部内容库

- **⑤网站知识（web）**：
  - Tab 1：🔗 外部网址（推荐，默认）— 抓取保持同步
  - Tab 2：📤 上传文件 — 静态文件
  - Tab 3：🗄️ 对象存储

- **⑥行业知识（industry）**：
  - Tab 1：🗄️ 对象存储（推荐，默认）— 统一管理
  - Tab 2：📤 上传文件
  - Tab 3：🔗 外部网址

- **①文档知识（document）**：
  - Tab 1：📤 上传文件（推荐，默认）
  - Tab 2：🔗 外部网址
  - Tab 3：🗄️ 对象存储

Tab导航位于板块选择器下方，当前Tab带蓝色下划线高亮，推荐Tab标注"推荐"蓝色徽章。切换板块时自动切到该板块的默认Tab。Tab下方直接显示对应的表单内容，无需二次点击。

**从前台页面选择**：后台拿到的是前台网站结构，而不是让运营理解内部生成过程。选择器按站点和完整导航树展示全部前台页面，每个页面展示页面 ID、标题、路由、导航路径、页面类型、页面数据类型、绑定数据和结构化字段。勾选页面入库后，`frontendPageId`、`frontendRoute`、`frontendNavPath`、`pageType`、`sourceDataMode`、`structuredFields` 写入知识资产元数据；导航结构默认参与学习，用来帮助智能客服判断用户问的是哪个页面的信息。

**筛选与多选**：

- 筛选项：名称搜索、板块、来源方式、状态、行业、时效、权限（7个维度）
- 支持多选行（checkbox）：表头全选/单行勾选
- 批量状态变更：仅当所选知识状态一致时可用，显示合法的下一步动作按钮（如"批量发布""批量提交评估"），操作区域出现在表单上方，状态不一致时提示"所选知识状态不一致，无法批量操作"
- 知识健康体检：目录页顶部用一个合并指标面板承载原体检页和原命中率 KPI，按“内容库存”与“前台问答效果”分组展示知识总数、待更新、最近更新时间、待补 FAQ、知识库召回率、AI 回复率、回复率损耗、总检索次数、命中次数、未回答数；点“新建体检任务”后，列表进入体检范围选择状态，筛选条件与 checkbox 都只服务本次任务，支持全选当前筛选、全选全部、清空选择；确认后创建后台任务。每条知识行内提供“加入体检”，用于预选一条知识后继续补充范围。体检结果不再作为临时列表，而是沉淀在“体检记录”中；已完成任务可回看结果和诊断时间。结果维度包括是否有用、低复用、需更新、无命中、缺前台路由、缺结构化字段；结果详情内提供补路由、补结构化字段、同步、重新学习、标记已更新、下线等动作，让运营能直接把知识修复到更健康的状态。

列表本身是全板块统一视图，靠"知识目录"人工文件夹分组（新建、归类）和筛选条件做二次归类，互不冲突——文件夹是运营自定义的业务分组，板块是系统固定的处理方式分类。命中率 KPI 合并进"知识健康体检"面板，"未回答问题转移"区块（原 N5 知识库页的能力）也整合在这里，形成"传内容 + 看效果 + 转化未回答问题"的一体化工作台。

### 7.4 知识详情

点开一条知识后看到的是这条内容的完整档案：元数据、健康度四维分解（质量/时效/复用/反馈，带改善建议）、更新历史时间线、同步信息（如果是需要保持更新的来源类型）。

核心是**内容双栏预览**：左边原文，右边是解析出来的切片，参照 RAGFlow 的交互设计——右侧切片列表可以搜索筛选，勾选后批量启用/禁用/删除；点开单个切片弹出编辑框，可以改正文、改类型标注、加关键词/可能的提问方式/标签、单独启用禁用。这一层是运营人工介入"解析质量"的最后手段：自动解析不理想时，不用重新调整板块级参数重跑，可以直接在这一条上手工修正；切片的"问题"数量也直接影响该资产的质量健康度分项（见 §8.1）。

切片被编辑、禁用或删除后，资产标记为“需重新学习”；重新学习成功前，线上检索继续使用上一成功版本，不能因为编辑未完成而返回半更新内容。详情中的元数据、状态、健康度、同步信息、门禁和更新历史必须同一版本刷新。

顶部操作区的状态操作不是摆设——点击"发布""下线""标记已更新"等按钮会真实改变这条知识的生命周期状态，并联动重算健康度（发布/标记已更新会把"最近更新时间"刷新为当天，从而让时效分项回到满分）。合法的下一步动作跟着当前状态走（见 §8.2），不能跳步骤。

入库前门禁检查逐项列出通过情况，不通过的给出原因和"重新学习"的修复路径。检索参数只做摘要展示并给出跳转配置入口，不重复放编辑控件。

### 7.5 元数据规范

四部分职责：一是全平台通用的元数据枚举维护（行业、业务用途、场景、门户网站），改一处全局下拉一致；二是「板块元数据总览」表——把 6 个板块的关键运营指标（条数、健康度、引用次数、自定义字段数、最近更新）放进一张可排序的表，方便运营快速定位"哪个板块最常用、最该投入维护资源"，而不用逐个板块页面去看，引用次数可切时间范围（默认"全部"，避免短窗口把长期重要但近期没被问到的板块埋没）；三是「健康度评分权重」配置——四个维度的计算口径是固定的（见 §8.1），但四项各占多少权重是运营可调的治理参数，调整时下方有实时预览，看得到"这个权重下 6 个板块各自平均分变成多少"，而不是盲调；四是「统一元数据字段」——系统内置字段只读展示，因为它们和状态机、健康度、检索逻辑绑定，改名字或删掉会让整个系统对不上；自定义字段则完全开放增删改，全部板块通用，一旦加入就会在「知识资产目录」编辑弹窗自动多出对应的输入框，填的值也会出现在「知识详情」的元数据卡片里——不是写文档用的静态表，是真的能逐条知识去维护的字段。

枚举新增时去除首尾空格且不允许同值重复；行业、业务用途、适用场景仍被资产使用时不可删除，需返回关联资产数。未选择门户网站表示对当前客户全部站点生效。自定义字段名只允许小写字母、数字和下划线，且全局唯一；已存在资产值的字段需先迁移或清空后才能删除。健康度配置的预览只改页面展示，保存后才按 §8.1 的规则参与后续重算。

### 7.6 同步状态

管理"内容进来之后怎么保持更新"这件事。

**默认展示近一周 + 更多按钮**：页面默认只展示近一周有同步记录的内容，减少信息过载；右上角"更多"按钮点击后查看全部历史记录，再次点击恢复为"仅显示近一周"。

**字段与知识资产目录保持一致**：统一列表展示，字段包括：内容标题、板块、来源方式、状态、健康度、频率（定时同步可调）、上次同步、下次同步、同步状态。点击表格行跳转至对应知识详情页，减少维护成本，用户体验更一致。

**筛选条件**：板块、来源方式、同步状态三个维度，与知识资产目录的筛选逻辑保持一致。

**日志查看**：参考 RAGFlow 的日志展示，黑色终端风格弹窗，显示同步过程的详细日志（开始时间、解析文档、切片处理、成功/失败状态、建议操作）。

**同类型多选批量操作**：同步内容列表支持勾选多条内容，但只能同时选择同一同步类型（实时同步 / 定时同步 / 手动），避免把实时监听、定时抓取、手动上传三种机制混在一次操作里造成误解。选中后顶部出现批量操作条：所有类型都支持"批量立即同步/批量重试"；定时同步类型额外支持批量调整频率（每小时/每日/每周等）。如果已经选择了某一类型，其他类型的 checkbox 禁用，用户需先清空选择再切换类型。

按同步机制分三组概览卡片：前台页面内容是实时同步（发布即更新）、外部网址和对象存储是定时抓取、上传文件是手动更新（重新上传触发）。状态分布 KPI（正常/同步中/失败/未启用）给运营一个总体健康快照，不用逐组去数失败项；有失败会置顶红色提示条。

### 7.7 问答测试

给运营一个"像用户一样提问"的沙盒，验证板块调优是否有效。新增的"测试范围"板块多选，是配合「知识板块」配置页调优流程的关键能力——怀疑是某个板块召回不准时，可以把测试范围收窄到只测那个板块，排除其它板块内容的干扰，看结果更聚焦。每条回答展示引用来源、相似度和相关性/权威性/时效性多维评分，帮助判断"答得对不对、可信不可信"，而不只是"有没有答案"。

默认范围为全部板块；取消最后一个已选板块时恢复为全部板块。严格度固定映射为宽松 0.60、标准 0.75、严格 0.85，只展示相似度达到当前阈值且属于测试范围的引用。回答风格只改变测试回答的表达，不改变检索结果；本页的提问和清空操作均不写入正式对话、复用、满意度或覆盖盲区统计。

### 7.8 对话质量运营

当前客户后台的对话分析页，数据来自该客户智能客服前台的一条条对话。页面回答"哪些用户问题没有命中知识、哪些对话没有回复、哪些回答被点踩、下一步该补哪类知识"。它不是平台跨客户看板，不提供客户/行业筛选；筛选维度是时间、场景（在线客服/网站搜索/内容生成）和命中知识名称。平台侧跨客户、跨行业的质量诊断统一放在 N12「平台运营看板」中承载。

**核心 KPI**：

- **对话轮次**：当前筛选范围内的智能客服对话/问题轮次数。
- **知识库召回率**（原"命中率"）：检索是否命中相关知识。
- **AI 回复率**：AI 是否最终给出回复。
- **RAG 准确率**：命中且已回复，并且没有点踩、没有转人工，作为"答得对"的代理指标。
- **回复率损耗归因**：解释召回率 → 回复率之间的损耗来自哪里（知识缺失/召回低/规则拦截/模型拒答/系统异常）。
- **满意度**（原"好评率"，拆分为满意度/点踩率）：只统计有明确赞踩反馈的记录。
- **覆盖盲区数**：当前待处理快照。

**页面内展示指标计算规则**：每个 KPI 旁边或下方展示公式，避免运营只看到数字不知道来源。详细公式见 §8.3.1。

**筛选维度**：

- 时间范围：今天/近7天/近30天/全部，决定对话轮次、召回率、回复率、准确率、满意度等事件类指标的统计口径。
- 场景：在线客服 / 网站搜索 / 内容生成。
- 命中知识名称搜索：用于定位某条知识被哪些对话引用、用户反馈如何。

**调用记录 + 未回答：默认近一周 + 更多按钮**

- **对话记录**：默认展示近一周的对话记录，右上角"更多"按钮点击后查看全部历史。表格包含日期/场景/用户问题/命中知识/相似度/反馈/是否回复/未回复原因/是否转人工/引用片段，可按场景筛选、按命中知识名称搜索，点击"原始对话"查看完整对话上下文。
  - **对话处理信息**：
    - 是否回复
    - 未回复原因：知识缺失、召回低、规则拦截、模型拒答、系统异常
    - 是否转人工
    - 引用片段信息，用于辅助判断 RAG 准确率

- **覆盖盲区（未回答问题）**：默认展示近一周的未回答问题聚类，右上角"更多"按钮查看全部。每条盲区包含：用户在问什么、本客户出现次数、影响会话数、最近提问时间、建议补到哪个板块、处理状态（待处理/已补录/已忽略）、优先级。
  - **盲区闭环信息**：
    - 保留按客户拆分的影响明细，但当前客户后台只读取当前客户自己的出现次数和影响会话数
    - 增加建议补录板块
    - 增加处理状态：待处理、已补录、已忽略
    - 增加优先级：由规则服务返回，页面仅展示，不在前端二次计算
    - 按优先级降序、最近提问时间倒序排序，优先处理高优先级未命中问题
  - **补录操作**：点击"补录"按钮，跳转至知识资产目录，预置板块为"问答知识"（faq），并带上问题内容作为参数，运营可直接填写答案完成补录。
  - **忽略操作**：点击"忽略"按钮，标记该问题不再提示补录（适用于超出业务范围或不需要回答的问题）。

顶部时间范围（今天/近7天/近30天/全部，默认近7天）决定了对话轮次/知识库召回率/AI 回复率/RAG 准确率/满意度这些 KPI 的统计口径，与对话记录表算的是同一批数据，确保数据一致性；覆盖盲区数是当前待处理快照，不随时间范围变化。形成"发现盲区 → 补充知识 → 召回率提升"的运营飞轮。

页面中的“知识健康体检”入口跳至知识资产目录后，应直接进入体检范围选择状态；“补录”应预置建议板块和问题主题，“忽略”后该主题立即不再计入待处理盲区和待补 FAQ。

### 7.9 目录内知识健康体检

知识健康体检不再是独立页面，而是「知识资产目录」里的常驻能力。目录顶部用一个合并指标面板保留原健康体检页和原命中率卡片的可视化信息：内容库存组展示知识总数、待更新、最近更新时间、待补 FAQ；前台问答效果组展示知识库召回率、AI 回复率、回复率损耗、总检索次数、命中次数、未回答数。

体检交互分三层：第一，点击"新建体检任务"后，目录列表进入体检范围选择状态，运营可利用现有筛选项缩小范围，再勾选知识、全选当前筛选或全选全部；第二，填写任务名称并点击"加入体检任务"，系统异步执行检测，任务状态为"体检中"或"已完成"；第三，从"体检记录"回看任意已完成任务，按任务查看检测范围、创建/完成时间、每条知识的页面上下文、检查前健康度、当前健康度、问题原因和处理建议。

任务创建后，检测范围、筛选条件和检测时间固定保存，不因后续筛选或新增资产变化。每条知识只给一个主结果，按“缺路由 → 缺结构化字段 → 需更新 → 无命中 → 低复用 → 有用”的顺序判断；因此同时缺路由和字段时显示“缺路由”。修复后可刷新当前健康度和当前结果，但保留当次检测时间、检测前健康度和原问题原因，确保体检记录既能回溯也能看修复进度。

查看详情不是只读报告，必须提供可交互修复动作。不同问题给不同动作：缺路由时可补齐前台页面路由和导航路径；缺结构化字段时可补齐字段；需更新时可立即同步、重新学习、标记已更新；低复用或无命中时可编辑元数据、重新学习或下线。动作执行后应刷新知识状态、更新时间、健康度和体检结果，让"发现问题 → 修复知识 → 再看健康度"形成闭环。

检测结果回答两类问题：第一，这条知识是否有用；第二，它能否正确支撑前台页面问答。页面展示 6 类结果：

- **有用**：近期被命中、健康度正常，继续参与检索。
- **低复用**：近期命中少或健康度偏低，建议补推荐问、补标签或合并到高频页面。
- **需更新**：内容过期或健康度低，建议同步前台页面并重新学习。
- **无命中**：近期没有对话命中，建议确认是否仍需参与检索。
- **缺路由**：来源是前台页面但没有 `frontendRoute`，无法判断用户问的是哪个页面。
- **缺结构化字段**：页面数据应按字段保存，但没有 `structuredFields`，影响精确问答和工具调用。

结果面板直接提供同步、重新学习、下线等处理动作。对话侧的召回、回复、满意度、原始对话明细继续在「对话质量运营」查看；覆盖盲区仍从对话质量页跳回知识资产目录补录。

---

## 八、核心指标计算口径

上一版 PRD 遗漏了这部分——健康度、状态机、调用记录这几处"数字/状态从哪来"没写清楚，导致原型里一度出现"两组手填数字凑数"、"状态改不了"、"调用记录没有日期、KPI 文案和数字对不上"这些不闭环的问题。本节把口径写清楚，方便研发、测试和产品评审统一理解。

### 8.1 健康度：四维分数怎么算，总分怎么加权

四个分项都按同一套规则从知识资产、解析切片和对话反馈中计算，输出 0–100：

| 维度 | 计算口径 |
|---|---|
| 质量 | 从 100 分开始扣分：资产名或任一切片命中敏感词时一次性扣 40 分；同板块存在疑似重复资产时一次性扣 20 分；有问题切片扣分为 `min(30, round(问题切片数 / 切片总数 × 100 × 0.3))`。三项相加后最低为 0 分。 |
| 时效 | 结构化数据因实时查询，默认满分；其他内容按距上次更新的时间与该内容的新鲜度要求比较，越久未更新分数越低。 |
| 复用 | 按统计窗口内被检索命中的次数计算，`min(100, 40 + 命中次数 × 12)`；统计窗口默认近 30 天，可在高级配置中调整。 |
| 反馈 | 统计窗口内有明确赞踩的命中记录。样本数小于最小样本量（默认 3）时给中性基准 75 分；达到样本量后，`round(点赞数 /（点赞数 + 点踩数）× 100)`。 |

新鲜度阈值按时效等级分级：实时类内容要求 1 天内更新，小时级内容要求 3 天内更新，每日级内容要求 14 天内更新，静态内容要求 180 天内更新。

**总分** = 四项按权重加权求和，默认权重质量 30 / 时效 25 / 复用 25 / 反馈 20，**可在「元数据规范」页调整**（四项滑块之和须为 100，调整后有实时预览）。草稿/待评估状态的资产尚未走完评估流程，健康度显示为"—"（不参与计算）。

板块级健康度 = 该板块所有已评估资产健康度的平均值，同一套规则自底向上算出，不是另外单独维护的数字。草稿、待评估和无健康度的资产均不进入分母。

### 8.2 生命周期状态机：谁能把状态从 A 改到 B

| 当前状态 | 允许的人工动作 | 触发方式 |
|---|---|---|
| 草稿 | 提交评估 → 待评估 | 人工（知识详情页 / 资产目录编辑弹窗按钮） |
| 待评估 | 发布 → 已发布；退回草稿 → 草稿 | 人工 |
| 已发布 | 下线 → 已归档 | 人工 |
| 待更新 | 标记已更新 → 已发布；下线 → 已归档 | 人工 |
| 已归档 | 恢复为草稿 → 草稿 | 人工 |

**唯一的自动转移**：已发布内容超过"2 倍新鲜度阈值"未更新，页面展示状态自动降级为"待更新"，因为"是否过期"是客观事实、不需要人工判断；发布、下线、归档这些需要业务判断的动作，不做自动化，必须人工点击。所有列表和详情页都以展示状态为准，保证"过期了但还没人工确认"的资产也能被看到并操作。

「知识详情」页和「知识资产目录」编辑弹窗都提供统一的状态操作组件（当前状态徽标 + 合法下一步按钮），不再是可以随意改成任意值的自由下拉——避免把"运营手滑改错状态"和"真实的业务流转"混为一谈。

### 8.3 调用记录与各类统计数字的时间维度

对话记录需要记录提问时间、场景、用户问题、命中知识、是否回复、未回复原因、是否转人工、用户反馈、原始对话内容，以及该对话是否关联某个覆盖盲区主题。这些信息是复用/反馈健康度计算、按日期/按文档筛选、以及从健康体检 Top 主题回溯原始对话的数据基础。

**对话处理信息**：
- AI 是否给出回复
- 未回复原因：知识缺失、召回低、规则拦截、模型拒答、系统异常
- 是否转人工
- 转人工原因
- 引用片段信息
- 用户反馈：点赞、点踩、无反馈

#### 8.3.1 对话质量运营指标计算规则

对话质量运营页所有事件类指标都来自当前客户智能客服前台对话。先套同一组筛选条件：时间范围（今天/近7天/近30天/全部）、场景、命中知识名称搜索。筛选后的每条记录代表一次用户提问及 AI 处理结果。

| 指标 | 分子 / 分母 | 公式 | 说明 |
|---|---|---|---|
| 对话轮次 | 筛选后的对话记录数 | 对话轮次 = 当前筛选范围内的用户提问次数 | 一次用户问题进入知识库检索/问答链路记 1 轮对话。 |
| 知识库召回率 | 命中知识的对话轮次 / 对话轮次 | 召回率 = 命中知识的对话轮次 / 对话轮次 × 100% | 衡量这轮对话是否找到相关知识。未命中会进入覆盖盲区聚类候选。 |
| AI 回复率 | 已回复对话轮次 / 对话轮次 | AI 回复率 = 已回复对话轮次 / 对话轮次 × 100% | 衡量这轮对话最终是否给出回答。即使召回成功，也可能因规则拦截、模型拒答、低置信度等原因不回复。 |
| RAG 准确率 | 命中知识且已回复、未点踩、未转人工的轮次 / 命中知识轮次 | RAG 准确率 = 准确命中次数 / 命中知识次数 × 100% | 原型阶段用"未点踩且未转人工"作为答得对的代理信号；后续可替换为人工质检/模型评测得分。 |
| 满意度 | 点赞轮次 / 有明确赞踩反馈的轮次 | 满意度 = 点赞数 /（点赞数 + 点踩数）× 100% | 无反馈记录不进分母，避免把沉默用户误算成满意或不满意。 |
| 点踩率 | 点踩轮次 / 有明确赞踩反馈的轮次 | 点踩率 = 点踩数 /（点赞数 + 点踩数）× 100% | 与满意度互补，用于快速定位差评对话。 |
| 回复率损耗 | 召回率与 AI 回复率的差值 | 回复率损耗 = 知识库召回率 - AI 回复率 | 用于解释"检索找到了，但 AI 没有答"的损耗。损耗原因按未回复原因分组统计：知识缺失、召回低、规则拦截、模型拒答、系统异常。 |
| 覆盖盲区数 | 当前客户仍待处理的问题聚类数 | 统计当前客户待处理盲区主题数量 | 快照类待办，不随顶部时间范围变化；每个盲区聚合当前客户相似未命中/未回复问题，并记录出现次数、影响会话数、建议板块和优先级。 |
| 盲区优先级 | 本客户出现次数、影响会话数、最近提问时间的综合排序 | 出现次数越多、影响会话越多、最近越常出现，优先级越高 | 目的是让运营先补"自己客户前台反复问、影响会话多"的问题。 |

**回复率损耗归因规则**：只统计 AI 没有回复的记录，根据未回复原因分组：

- 知识缺失：检索侧没有足够内容支撑回答，应进入补录任务。
- 召回低：可能有内容但相似度、阈值或切片导致没有稳定命中，应检查检索参数或切片。
- 规则拦截：通常是权限、敏感词、低置信度策略导致不回复。
- 模型拒答：通常是模型安全策略或无法组织可信答案。
- 系统异常：检索服务异常、编排失败或其他系统问题导致没有回复。

#### 8.3.2 对话质量运营「覆盖盲区」计算规则

对话质量运营里的「覆盖盲区」不是平台跨客户榜单，只看当前客户自己的待处理问题。它的业务含义是：客户前台用户反复问到某类问题，但智能客服没有答上来，系统把这些相似失败问题聚合成主题，按影响程度排序，方便运营优先补知识。

**入榜条件**：

1. 只展示当前仍待处理的主题；已经补录、已经忽略的主题不再进入 Top。
2. 只展示当前客户自己的问题，不展示平台其他客户的问题。
3. 只统计"没有回答上来"的失败对话，包括没有命中知识、低召回导致未回复、知识缺失导致未回复等情况。
4. 已经回复但用户点踩的记录不进入覆盖盲区，这类问题属于回答质量问题，应进入 RAG 准确率、满意度或低质量回答复盘。

**排序规则**：

先看该主题在当前客户前台出现了多少次，出现越多越靠前；如果出现次数相同，再看影响了多少个会话；如果仍然相同，最近被问到的主题排在前面。页面默认展示近一周，可通过「更多」查看全部。

**字段展示**：

| 列 | 说明 |
|---|---|
| 问题主题 | 相似失败问题聚合后的主题名，例如"私有化部署支持"。 |
| 本客户出现次数 | 当前客户前台用户问到该主题的次数。 |
| 影响会话数 | 被该主题影响的去重会话数，用于避免同一会话反复追问被过度放大。 |
| 最近提问 | 当前客户最近一次问到该主题的时间。 |
| 建议板块 | 系统建议补录到哪个知识板块，例如问答知识、行业知识、内容资产。 |

**闭环动作**：

- **查看对话**：打开该主题关联的原始对话样本，展示用户问题、机器人是否回复、是否命中知识、未回复原因等，帮助运营判断为什么没有答上来。
- **去补录**：跳转到知识资产目录，默认带入建议板块和问题主题，运营补充答案或资料后，该主题从待处理变为已补录。
- **忽略**：如运营判断该问题超出业务范围或不需要回答，可忽略该主题，忽略后不再进入健康体检 Top。

待补 FAQ 数量统计当前客户待处理主题中，建议补到问答知识板块的数量。该指标是快照类待办，不随对话质量运营页顶部时间范围变化。

统计数字分两类，处理方式不同：

- **快照类**（当前状态，不需要时间范围）：概览 KPI、同步状态的方式概览/状态分布、知识资产目录的召回率 KPI、对话质量运营的"覆盖盲区"数——这些是"现在的库存/待办情况"，不是"这段时间发生的事"，加时间筛选没有意义。
- **事件计数类**（需要时间范围，本轮补上）：对话质量运营的对话轮次/知识库召回率/AI 回复率/RAG 准确率/满意度/点踩率/回复率损耗——提供"今天/近7天/近30天/全部"筛选（默认近7天），KPI 卡片的时间文案跟着选择动态显示，不再写死"近7天"却按全量数据计算；对话记录表新增按命中知识名称搜索。元数据规范「板块元数据总览」的"引用次数"列同样可切时间范围，默认给"全部"（这张表是横向排序找高频板块用的，默认时间窗口太短会把长期重要但近期没被问到的板块埋没）。

### 8.4 入库前清洗与质量评估策略：每一项判定都有具体依据

板块配置页「入库前清洗与质量评估策略」卡片下的每个开关/阈值，都对应一套**可解释的判定规则**，不是摆设的 toggle；这套规则同时驱动「知识板块」配置页的开关状态、「知识详情」页的门禁检查结果、以及 §8.1 健康度"质量"分项——三处读的是同一组事实，不是三份互相不知道对方的数据。

| 配置项 | 判定依据 |
|---|---|
| 敏感词过滤 | 扫描资产名称和全部解析切片正文，命中「元数据规范」页维护的敏感词库中的任意词即判定不通过，并列出命中的具体词。 |
| 去重 | 一期只在同板块内比较标题与标签，不做全文语义去重：标题按分词集合计算 Jaccard 相似度，并为每个相同标签增加 0.1；综合相似度大于等于阈值（默认 0.60）即判定疑似重复，并返回相似资产与分数。 |
| 打分策略（质量评分拦截阈值） | 质量打分从 100 分开始扣分：敏感词命中、疑似重复、切片解析问题都会扣分；低于阈值时，门禁检查中的"质量打分是否达标"不通过。 |
| 答案准确性人工审核（②问答知识专属） | 唯一不能自动判定的一项，由运营在知识资产目录或知识详情页手动标记；未标记人工审核时，不允许直接通过该项门禁。 |

门禁检查在原先 6 项基础上新增第 7 项"质量打分是否达标"，让打分阈值真正在门禁里起作用；「知识详情」页的"入库前门禁检查"卡片每一行下方都展示这行依据文本（比如"命中：绝对、保证"或"与某条资产相似度较高"），不通过时额外给出处理建议。门禁结果必须从真实内容信号得出，不能靠固定样例硬编码。

---

## 九、核心业务对象

| 业务对象 | 说明 |
|---|---|
| 知识板块 | 系统内置的 6 类知识分类，决定内容的处理方式、清洗策略、检索策略和运营统计口径；运营不可新增或删除板块，只能配置各板块的处理策略。 |
| 知识资产 | 知识资产目录里的核心对象，一条记录代表一份知识内容；包含来源方式、所属板块、适用门户网站、行业、业务用途、场景、时效、权限、生命周期状态、健康度、标签、备注、同步信息等。来源为前台页面时还必须关联页面 ID、路由、导航路径、页面类型、数据类型与结构化字段。 |
| 解析切片 | 知识详情页"内容"区域的最小管理单元；运营可查看、搜索、编辑、启用/禁用切片，切片问题会影响资产质量分。 |
| 门户网站 | 同一客户后台下的多语言或地区站点，知识可选择适用哪些站点；未选择时默认全部站点可用。 |
| 更新历史 | 记录知识创建、编辑、重新学习、同步、状态变更等操作，供运营回溯。 |
| 对话记录 | 智能客服前台的用户提问与 AI 处理结果，是对话质量运营、健康度反馈分、引用次数、未命中主题回溯的基础。 |
| 覆盖盲区 | 从未命中或未回复对话中聚类出的待补主题，包含出现次数、影响会话数、最近提问、建议补录板块和处理状态。 |
| 健康度分解 | 质量、时效、复用、反馈四个维度的分数及权重，用于解释知识健康度总分。 |
| 门禁检查结果 | 入库前清洗与质量评估的结果，包括是否通过、判定依据和不通过时的处理建议。 |
| 敏感词库 | 元数据规范页维护的合规检查词库，供各板块的敏感词过滤策略使用。 |

---

## 十、当前范围说明

- 当前原型用于确认页面结构、交互流程、指标口径和运营闭环；真实数据接入、权限、登录、审批流转等能力不在本轮原型范围内。
- 未覆盖：权限与登录、审批流转、多租户隔离（已在架构层面明确改为"多门户网站"而非"多租户"，见《知识库体系架构方案》）、真实的向量检索/RAG 后端。
- 页面文案已刻意去除"分块算法/向量库/RAG"等技术黑话，仅在配置页面向愿意深入的运营人员提供参数级控制。

---

## 十一、测试用例

以下用例为本期最小回归集；测试数据需可构造固定时间、固定命中次数、固定赞踩数和固定前台页面字段，以验证边界值。

| 编号 | 场景与前提 | 操作 | 预期结果 |
|---|---|---|---|
| TC-KB-001 | 前台页面有 `pageId`、路由、两级导航和 3 个结构化字段 | 在选择器勾选页面并导入 | 新建一条前台页面资产；路由、完整导航路径、页面类型、数据类型、字段均写入元数据；导航上下文可查看。 |
| TC-KB-002 | 已导入 `siteId+pageId` 的页面，标题和路由已变化 | 再次导入该页面 | 更新原资产并写更新历史，不新增重复资产。 |
| TC-KB-003 | 前台页面无路由 | 导入后发起体检 | 导入成功但有字段告警；结果为“缺路由”，不可判为“有用”。 |
| TC-KB-004 | 结构化数据资产无字段 | 保存并发起体检 | 允许保存为待补齐；结构化字段门禁失败，体检结果为“缺结构化字段”。 |
| TC-KB-005 | 目录存在不同板块、状态和行业资产 | 同时设置关键字、板块、行业筛选 | 只返回同时满足全部条件的资产；关键字不区分大小写。 |
| TC-KB-006 | 筛选结果 3 条、目录总数 10 条 | 点“全选当前筛选” | 仅选择 3 条；切换筛选后已选总数仍正确。 |
| TC-KB-007 | 已发布与待更新资产同时被选择 | 查看普通批量操作栏 | 不显示非法的批量状态迁移动作。 |
| TC-KB-008 | 体检范围状态、当前筛选 3 条 | 点“全选当前筛选”后创建任务 | 任务范围为 3 条，保存筛选和资产快照；普通批量操作不出现。 |
| TC-KB-009 | 未选择任何资产 | 点击“加入体检任务” | 不创建任务，给出至少选择一条的提示。 |
| TC-KB-010 | 已创建体检任务 | 在任务运行时打开记录 | 状态为体检中；不能查看未完成的结果。 |
| TC-KB-011 | 任务完成后资产被删除 | 打开历史任务 | 保留该资产名称和结果快照，标明资产已删除，不产生跳转异常。 |
| TC-KB-012 | 前台页面无路由且无结构化字段 | 发起体检 | 主结果为“缺路由”，验证优先级高于“缺结构化字段”。 |
| TC-KB-013 | 已发布资产距更新时间超过两倍阈值，且无命中 | 发起体检 | 主结果为“需更新”，验证优先级高于“无命中”。 |
| TC-KB-014 | 已发布资产近 30 天 0 命中、健康度 80 | 发起体检 | 主结果为“无命中”，建议包含编辑元数据、重新学习、下线。 |
| TC-KB-015 | 已发布资产近 30 天 1 命中、健康度 74 | 发起体检 | 主结果为“低复用”。 |
| TC-KB-016 | 已发布资产近 30 天 2 命中、健康度 80、字段完整 | 发起体检 | 主结果为“有用”。 |
| TC-KB-017 | 已完成体检结果为缺路由 | 补齐路由并保存 | 更新历史生成；当前结果刷新为下一匹配分类；原检测时间、原主结果、`healthBefore` 保留。 |
| TC-KB-018 | 资产同步任务失败 | 查看资产、同步页和体检详情 | 不更新内容 `updatedAt`；同步状态为失败，展示原因且可重试。 |
| TC-KB-019 | 资产同步成功 | 完成同步 | 更新 `lastSync`、`updatedAt`、门禁/健康度当前值和体检当前状态。 |
| TC-KB-020 | 质量：命中敏感词、存在重复、10 个切片中 5 个有问题 | 计算质量分 | `Q=max(0,100-40-20-round(5/10×100×0.3))=25`。 |
| TC-KB-021 | 时效实时资产更新于 1 天前、2 天前、3 天前 | 分别计算时效分 | 分别为 100、50、40；验证分段临界点。 |
| TC-KB-022 | 近 30 天命中数分别为 0、1、5 | 计算复用分 | 分别为 40、52、100。 |
| TC-KB-023 | 反馈样本为 2 条、点赞 1 点踩 1；最小样本为 3 | 计算反馈分 | 返回 75，不使用 50%。 |
| TC-KB-024 | 反馈样本为 4 条、点赞 3 点踩 1 | 计算反馈分 | 返回 75。 |
| TC-KB-025 | 权重和为 95 | 保存健康配置 | 服务端拒绝保存并指出权重和必须为 100。 |
| TC-KB-026 | 草稿和待评估资产 | 查看列表、板块平均和低质数量 | 健康度均为 `--`，不进入平均健康度和低质计数。 |
| TC-KB-027 | 已发布非结构化资产距更新日严格大于两倍阈值 | 查询目录并筛选待更新 | 有效状态显示待更新；等于两倍阈值时仍显示已发布。 |
| TC-KB-028 | 板块关闭敏感词门禁 | 资产命中敏感词后查看门禁与质量分 | 该项显示未启用，而非通过；敏感词扣分为 0。 |
| TC-KB-029 | 两条同板块资产标题 token Jaccard 0.5，共享 1 个标签 | 阈值为 0.60 时运行去重 | 综合分 0.60，判为疑似重复并返回对方资产。 |
| TC-KB-030 | 定时同步资产与手动资产 | 在同步页先选定时资产，再选手动资产 | 阻止混选，说明批量操作仅支持同一同步机制。 |
| TC-KB-031 | 近 7 天默认同步视图，某资产最后同步 8 天前 | 打开同步页，再点查看更多 | 默认不显示；查看更多后显示。 |
| TC-KB-032 | 100 条有效对话，70 条命中，60 条回复 | 查看目录面板与质量运营 | 召回率 70%，回复率 60%，损耗 10 个百分点；两页同口径。 |
| TC-KB-033 | 无有效对话 | 查看事件类比率 KPI | 比率显示 `--`，总检索次数、命中次数、未回答数为 0。 |
| TC-KB-034 | 覆盖盲区主题已去补录或忽略 | 查看待补 FAQ 和盲区列表 | 主题不再出现在待处理列表，也不计入待补 FAQ。 |
| TC-KB-035 | 问答测试设为严格且只选某板块 | 输入仅在其他板块命中的问题 | 不返回其他板块引用；不向正式对话、命中统计和盲区写入事件。 |
| TC-KB-036 | 修改板块解析/检索配置 | 不重新学习直接测试已有资产，再重新学习后测试 | 前者使用旧解析结果；后者任务成功后使用新配置与新切片。 |
| TC-KB-037 | 概览有 2 条待评估、3 条待更新、4 条低质资产，其中 1 条同时待更新且低质 | 查看“待处理” | 显示 9，并明确为分类合计；待办项可分别跳转到对应处理范围。 |
| TC-KB-038 | 点击知识板块卡片和配置图标 | 分别操作两个入口 | 卡片仅打开预置板块筛选的目录；配置图标仅打开对应板块配置，互不串行。 |
| TC-KB-039 | 结构化数据板块 | 打开板块配置页 | 不展示切片、清洗、检索参数表单；仅展示使用说明与只读内容概览。 |
| TC-KB-040 | 文档板块配置中的文本块大小填入 63、65、2112 | 保存配置 | 三次均拒绝保存并提示合法范围与 64 的步长；填入 128 可保存。 |
| TC-KB-041 | 自动元数据字段存在重名或空字段名 | 保存板块配置 | 服务端拒绝，并定位重复或空字段。 |
| TC-KB-042 | 已发布资产的一个切片被禁用或删除 | 查看资产状态并发起重新学习 | 资产标记需重新学习；任务成功前线上检索仍使用上一成功版本。 |
| TC-KB-043 | 元数据规范中某行业已被 2 条资产使用 | 删除该行业 | 删除被拒绝，返回关联资产数；不改变资产已有字段。 |
| TC-KB-044 | 元数据规范未选择门户网站 | 保存并打开资产编辑 | 表示当前客户全部站点生效，不产生空站点范围。 |
| TC-KB-045 | 修改健康度权重后仅查看预览 | 刷新资产目录和详情 | 预览值可变化，但未保存前资产健康度不变；保存后按重算规则生效。 |
| TC-KB-046 | 同步列表有近一周与 8 天前记录 | 点击查看更多，再设置来源方式筛选 | 仅列表时间范围变化；筛选条件为且关系；行内日志操作不跳转详情。 |
| TC-KB-047 | 问答测试取消最后一个选中板块 | 继续发送问题 | 自动恢复全部板块；按当前严格度过滤，不写入运营指标。 |
| TC-KB-048 | 对话质量运营已设置近 30 天和网站搜索 | 点击查看更多、查看原始对话、点击健康体检入口 | 筛选保留；原始对话信息完整；体检入口进入目录的体检范围状态。 |
| TC-KB-049 | 待处理盲区点击补录或忽略 | 分别操作 | 补录预置建议板块和问题主题；忽略后立即不计入待补 FAQ 和待处理盲区。 |

---

## 十二、验收标准与明确不在范围

### 12.1 验收标准

1. 前台页面资产可从完整网站导航中选择，且每条资产均可追溯到站点、页面、路由、导航路径与页面数据形态。
2. 目录内体检支持按筛选结果、全部资产和单条资产建立异步任务；任务范围可追溯、结果可查看、修复动作可闭环，不存在独立的“知识健康体检”导航或路由。
3. 同一资产在目录、详情、体检任务、概览和元数据规范中的状态、健康度、命中次数必须一致；本节测试用例全部通过。
4. 所有指标有明确分子、分母、统计窗口、空值表现和边界值；后端服务端计算通过固定测试数据校验，前端不得产生第二套口径。
5. 修复操作完成后，目录、详情、同步状态和历史体检结果的当前状态在一次刷新内一致；原任务检测快照不被覆盖。
6. 概览、知识板块、板块配置、资产目录/详情、元数据规范、同步状态、问答测试、对话质量运营均按第六、七节描述实现；跨页面跳转携带必要筛选或上下文，不丢失业务动作。

### 12.2 本期不在范围

- 不建设或暴露页面设计器、CMS 栏目编辑、页面布局编辑能力；本模块只消费“前台网站结构/前台页面”结果。
- 不实现全文语义查重、跨板块去重、版权自动裁决或 AI 自动改写知识；一期去重仅使用标题与标签相似度。
- 不建设登录、角色权限、审批、多租户隔离、真实向量检索引擎、模型编排和生产级调度重试策略；接口需预留操作者与站点/客户上下文。
- 不把问答测试记录计入真实运营指标、资产复用或覆盖盲区。
- 不自动发布、自动下线或自动删除知识；所有会改变业务可用性的状态迁移均由用户明确触发。
